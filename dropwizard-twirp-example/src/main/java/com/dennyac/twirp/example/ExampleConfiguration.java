package com.dennyac.twirp.example;

import io.dropwizard.core.Configuration;

/**
 * Configuration for the Haberdasher example. Empty for now — we don't have
 * anything to configure, but Dropwizard requires a Configuration subclass.
 */
public class ExampleConfiguration extends Configuration {
}
